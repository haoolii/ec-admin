import React, { ReactElement } from 'react';

interface Props {
    
}

export default function Product({}: Props): ReactElement {
    return (
        <div>
            <h1>Product</h1>
        </div>
    )
}

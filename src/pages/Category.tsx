import React, { ReactElement } from 'react';

interface Props {
    
}

export default function Category({}: Props): ReactElement {
    return (
        <div>
            <h1>Category</h1>
        </div>
    )
}
